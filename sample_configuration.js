const myApiKey = "your maps api key here";

function getApiKey() {
    return myApiKey;
}

// Boston University Central
const latitude = 42.3500
const longitude = -71.1071

function getLatLng() {
    return {"lat": latitude, "lng": longitude};
}
